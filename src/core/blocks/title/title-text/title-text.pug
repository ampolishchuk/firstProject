mixin title-text()
    - var params = {}
    - params.mod = 'title-text'
    - params.value = ''
    - params.tag = 'span'

    if attributes.params
        +mergeObj(params, attributes.params)

    #{params.tag}(class=params.tag+' '+params.mod)
        | #{params.value} 
        block